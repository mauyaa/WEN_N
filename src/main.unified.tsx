import React from 'react';
import ReactDOM from 'react-dom/client';
import WennUnifiedApp from './App.unified';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WennUnifiedApp />
  </React.StrictMode>,
);
