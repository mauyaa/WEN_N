import React from 'react';
import ReactDOM from 'react-dom/client';
import WennApp from './App.wenn';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WennApp />
  </React.StrictMode>,
);
