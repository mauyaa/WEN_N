import React from 'react';
import ReactDOM from 'react-dom/client';
import WennCommandApp from './App.command';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WennCommandApp />
  </React.StrictMode>,
);
